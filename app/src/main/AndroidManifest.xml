<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firriezky.submission1_intro">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_book"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_book"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submission1_Intro">
        <activity android:name=".View.MyProfileActivity"/>
        <activity android:name=".View.DestinationActivity" />
        <activity android:name=".View.RecyclerActivity" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>